# Colcon (ROS 2) workspace artifacts
ros/build/
ros/install/
ros/log/

# direnv
.direnv/

# Python
__pycache__/
*.pyc

# Training artifacts — checkpoints, tensorboard logs, replay buffers.
# Models are regenerable from train.py; logs are per-run.
training/models/
training/runs/

# Claude Code local agent state
.claude/
